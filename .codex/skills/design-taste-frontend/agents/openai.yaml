interface:
  display_name: "Taste Skill V2"
  short_description: "Studio-grade websites with imagery, components, and motion"
  default_prompt: "Use $design-taste-frontend to build a distinctive responsive website with substantial components, project-bound imagery, a polished motion system, and rendered visual QA."

dependencies:
  tools:
    - type: "mcp"
      value: "taste-blocks"
      description: "Verified Taste Blocks component discovery and registry payloads"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
